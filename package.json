{
  "name": "blogging-website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    // Your dependencies
  },
  "devDependencies": {
    "parcel": "^2.13.3"
  }
}
